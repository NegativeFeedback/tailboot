{
  "name": "tailboot",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "pnpm test && tsc --noEmit",
    "build:cli": "tsc --noEmit && pnpm --filter @tailboot/cli build",
    "test": "node --test tailboot-iso.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^6.0.3"
  }
}
